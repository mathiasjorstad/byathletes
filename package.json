{
  "name": "byathletes",
  "version": "1.0.0",
  "description": "ByAthletes — automated newspaper builder",
  "scripts": {
    "build": "node build.js",
    "deploy": "node build.js && wrangler pages deploy . --project-name=byathletes --commit-dirty=true"
  }
}
